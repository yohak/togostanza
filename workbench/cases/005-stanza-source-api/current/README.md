# Current fixture for case 005

This fixture is a minimal current-version Stanza repository for observing the
Stanza base API used by existing Stanza source code.

The main probe stanza is `stanzas/api-probe`.
